# ── OpsRunbook RAG — Environment Configuration ────────────────────────────
# Copy this file to .env and fill in your values

# Groq API Key (get one free at https://console.groq.com)
GROQ_API_KEY=gsk_onnBqa8smK.........................

# PostgreSQL connection — set your own DB name and password
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=143abeesh@k

# Your custom database names (rename to whatever you like)
AUTH_DB_NAME=opsrunbook_auth
APP_DB_NAME=opsrunbook_app
